apiVersion: apps/v1
kind: Deployment
metadata:
  name: visits-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: visits-service
  template:
    metadata:
      labels:
        app: visits-service
    spec:
      containers:
        - name: visits-service
          image: 866481181845.dkr.ecr.us-east-1.amazonaws.com/petclinic-visits-service:latest

          env:
            - name: CONFIG_SERVER_URL
              value: "http://config-server:8888/"

          ports:
            - containerPort: 8082

          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"

---
apiVersion: v1
kind: Service
metadata:
  name: visits-service
spec:
  selector:
    app: visits-service
  ports:
    - port: 8082
      targetPort: 8082
  type: ClusterIP
